name: First workflow
on: workflow_dispatch
jobs:
 first_job:
  runs-on: ubuntu-latest
  steps: 
   - name: print greeting
     run: |
      echo "good morning"
      echo "hello again"
   - name: print bye
     run: echo "done"
 
