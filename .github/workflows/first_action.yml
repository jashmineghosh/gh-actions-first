name: First workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
        run: |
          echo "First output"
          echo "Second output"
      - name: print bye
        run: echo "done---"
